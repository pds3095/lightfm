services:
  lightfm:
    build:
      context: ../
      dockerfile: ./lightfm/Dockerfile
    # Uncomment this to mount your local version
    # of the LightFM code.
    # volumes:
    #   - /home/prabhu/:/home/
    ports:
    - "8888:8888"
